# Define here the models for your scraped items
#
# See documentation in:
# https://docs.scrapy.org/en/latest/topics/items.html

from dataclasses import dataclass


@dataclass
class WebScrapingItem:
    # define the fields for your item here like:
    # name: str | None = None
    # nom_jeu : str | None = field(default=None)
    # date_test : str | None = None = field(default=None)
    # meilleur_prix = float | None = None = field(default=None)
    # nom_website : str | None = None = field(default=None)
    # points_positifs : str | None = None = field(default=None)
    # points_negatifs : str | None = None = field(default=None)
    # note_redaction : float | None = None = field(default=None)
    pass