
print("""
    SELECT
        id,
        species_scientific_name,
        species_common_name,
        form,
        growth_rate,
        fall_color,
        environmental_tolerances,
        location_tolerances,
        notes_suggested_cultivars,
        tree_size,
        comments
    FROM tree_species;

""")